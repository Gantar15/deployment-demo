- name: Deploy and build server
  hosts: deploy
  roles:
    - role: pre_config
      tags: pre_config
    - role: deploy
      tags: deploy
    - role: build
      tags: build
    # - swarm_init

- name: Managers
  hosts: managers
  roles:
    - role: pre_config
      tags: pre_config
    # - swarm_join
